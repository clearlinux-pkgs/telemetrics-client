--- telemetrics-client-1.10.1/src/probes/crash_probe.c~	2016-08-17 21:09:33.000000000 +0000
+++ telemetrics-client-1.10.1/src/probes/crash_probe.c	2017-01-31 02:50:34.790575996 +0000
@@ -434,6 +434,12 @@
                 goto success;
         }
 
+	if (proc_path && strlen(proc_path) < 512) {
+		char command[4096];
+		sprintf(command, "/usr/bin/prime-debuginfo.sh %s", proc_path);
+		system(command);
+	}
+
         if (core_file) {
                 core_fd = open(core_file, O_RDONLY);
                 if (core_fd == -1) {
